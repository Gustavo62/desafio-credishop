<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Desafio Credishop" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_pack_tag 'application', 'data-turbo-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light mb-3">
      <div class="container-fluid">
        <%= image_tag("credishop_logo.png", :alt => "rss feed", class:"logo-navbar") %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item <%= 'active' if current_page?('/') %>">
              <%= link_to 'Home', root_path, class: "nav-link" %>
            </li>
            <li class="nav-item <%= 'active' if current_page?('/proponents') %>">
              <a class="nav-link" href="#">Proponentes</a>
            </li>
            <li class="nav-item <%= 'active' if current_page?('/discount_rates') %>">
              <%= link_to 'Bases INSS', discount_rates_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#">Sobre</a>
            </li>
          </ul>
          <form class="ml-auto d-flex">
            <input class="form-control me-3" type="search" placeholder="Busca de proponente" aria-label="Search">
            <button class="btn btn-outline-success" type="submit">Pesquisar</button>
          </form>
        </div>
      </div>
    </nav>
    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
